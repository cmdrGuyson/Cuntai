/* module.exports = {
	name: "daddy",
	description: "who's your daddy?",
	// eslint-disable-next-line no-unused-vars
	execute(message, command, args) {
		if(!args.length) {
			return message.channel.send(`Tell me what to do daddy!`);
		}
		message.channel.send(`**Command: ${command}\nArguments: ${args}**`);
	},
};
*/